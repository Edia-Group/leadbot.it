import { describe, expect, it } from "bun:test";

describe("@typebot.io/results", () => {
  it("has no package-level unit tests (export workflow tests removed with workflows app)", () => {
    expect(true).toBe(true);
  });
});
