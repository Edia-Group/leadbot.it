# Copy to `.env` at repo root (and optionally `apps/builder/.env` / `apps/viewer/.env` symlinks).

ENCRYPTION_SECRET=H+KbL/OFrqbEuDy/1zX8bsPG+spXri3S

DATABASE_URL=postgresql://postgres:leadbot@localhost:5433/leadbot

NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_VIEWER_URL=http://localhost:3001

ADMIN_EMAIL=admin@leadbot.it

S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=leadbot
S3_PORT=9000
S3_ENDPOINT=localhost
S3_SSL=false

NODE_OPTIONS=--no-node-snapshot

# Mailpit (docker-compose.dev.yml): SMTP 1025, web UI http://localhost:8025
NEXT_PUBLIC_SMTP_FROM="LeadBot <dev@localhost>"
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
