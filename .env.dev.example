# Copy to `.env` at repo root (and optionally `apps/builder/.env` / `apps/viewer/.env` symlinks).

ENCRYPTION_SECRET=H+KbL/OFrqbEuDy/1zX8bsPG+spXri3S

DATABASE_URL=postgresql://postgres:leadbot@localhost:5432/leadbot

NEXTAUTH_URL=http://localhost:3000
NEXT_PUBLIC_VIEWER_URL=http://localhost:3001

ADMIN_EMAIL=admin@leadbot.it

S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=leadbot
S3_PORT=9000
S3_ENDPOINT=localhost
S3_SSL=false

NODE_OPTIONS=--no-node-snapshot

# Render SMTP (replace with your Render credential values)
NEXT_PUBLIC_SMTP_FROM="LeadBot <onboarding@yourdomain.com>"
SMTP_HOST=smtp.render.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USERNAME=
SMTP_PASSWORD=
